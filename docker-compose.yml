services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080

  client:
    build:
      context: .
      dockerfile: apps/client/Dockerfile
    ports:
      - "5173:80"
    depends_on:
      - server
